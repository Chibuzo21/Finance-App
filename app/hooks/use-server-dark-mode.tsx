// import { cookies } from "next/headers";

// const useServerDarkMode = async (defaultTheme = "light") => {
//   return (await cookies()).get("theme")?.value ?? defaultTheme;
//   //   This means if cookies through the get method retrives any value from the name "theme" then this function returns that value but if it was undefined or no value could be traced or retrieved then the above function should return the default theme which is a string
// };

// export default useServerDarkMode;
